# SQL More Queries

This folder contains SQL scripts for the "SQL More Queries" chapter.  

## Requirements
- All files end with a new line.
- All SQL queries have a comment describing the task.
- SQL keywords are in uppercase.
- Scripts are executed on MySQL 8.0 (Ubuntu 20.04 LTS).
- The length of the files will be tested using `wc`.
